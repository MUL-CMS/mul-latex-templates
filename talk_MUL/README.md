# MUL Talk Template — v2.01 (2024)

pdfLaTeX, `beamerDMS.cls`, structure with `figs` and `graphics` (Department of Materials Science design).

## Compiling

```
make pdf
```

## What changed vs v2.00

- `\insertsection` → `\insertsectionhead` fix for ToC/lecture frame titles
- `\framegrid` spacing tweak (grid rows divided by 18 instead of 20)

---
Superseded by `v2.10-2024`. See [`CHANGELOG.md`](CHANGELOG.md) for the full version history. Frozen at tag `talk-v2.01-2024`.
