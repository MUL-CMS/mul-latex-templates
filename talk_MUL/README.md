# MUL Talk Template — v1.01 (2019)

pdfLaTeX, `beamerDMS.cls` (Department of Materials Science design).

## Compiling

```
make pdf
```

## What changed vs v1.00

- Introduces `figs/` and `graphics/` subfolders; logo paths updated to `graphics/logo_MUL_M`, `graphics/logo_MUL`

> Note: `beamerDMS.cls`'s internal `\ProvidesClass` string is unchanged from v1.00 (still reads `v1.00`).

---
Superseded by `v1.02-2020`. See [`CHANGELOG.md`](CHANGELOG.md) for the full version history. Frozen at tag `talk-v1.01-2019`.
