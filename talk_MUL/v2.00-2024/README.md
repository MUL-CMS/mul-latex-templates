# MUL Talk Template — v2.00 (2024)

pdfLaTeX, `beamerDMS.cls`, structure with `figs` and `graphics` (Department of Materials Science design). Major clean-up release.

## Compiling

```
make pdf
```

## What changed vs v1.02

- New format: 16:9 (from 16:10)
- New class options: `emptyFootings`, `fullFootings`
- Major internal clean-up of the class
- `overdraw`/`\framegrid` carried over from v1.02

`beamerDMS.cls`'s `\ProvidesClass` string is correctly bumped for this release (`2023/02/22 v2.00`).

---
Superseded by `v2.01-2024`. See [`../CHANGELOG.md`](../CHANGELOG.md) for the full version history. Frozen at tag `talk-v2.00-2024`.
